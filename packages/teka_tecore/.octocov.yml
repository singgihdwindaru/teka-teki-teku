# generated by octocov init
coverage:
  if: true
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
repository: andiasrafil/teka-teki-teku/packages/teka_tecore
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
