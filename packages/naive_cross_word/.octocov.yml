# generated by octocov init
coverage:
  if: true
  paths:
    - coverage/lcov.info
  acceptable: 10%
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
repository: andiasrafil/teka-teki-teku/packages/naive_cross_word
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
