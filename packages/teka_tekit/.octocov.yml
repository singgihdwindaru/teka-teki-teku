# generated by octocov init
coverage:
  if: true
  paths:
    - coverage/lcov.info
  acceptable: 10%
  badge:
    path: docs/coverage.svg
testExecutionTime:
  if: true
  badge:
    path: docs/execution_time.svg
codeToTestRatio:
  if: true
  badge:
    path: docs/code_to_test_ratio.svg
push:
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
repository: andiasrafil/teka-teki-teku/packages/cross_word
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
